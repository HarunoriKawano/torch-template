# PyTorch & MLflow 実験管理プロジェクト規約

## 1. プロジェクト構造とファイル分割
責務を明確に分離し、可読性を高めるため以下の構造を標準とします。モデル定義とデータ処理は別ファイルとして隔離し、学習プロセスから参照して使用します。

    project_root/
    ├── configs/           # 実験設定ファイル（YAML等）
    ├── src/
    │   ├── models/        # モデル定義（本規約の対象外・別管理）
    │   ├── data/          # データセット・ローダー定義
    │   ├── training/      # 学習ループ、評価ループ、MLflowロジック
    │   └── utils/         # 再現性確保、ロギング、チェックポイント管理などの補助関数
    ├── scripts/           # 実行用エントリポイント（train.py など）
    ├── outputs/           # MLflowのローカル保存先（自動生成）
    ├── notebooks/         # Jupyter notebook保存先
    └── data/              # データセットの保存先 (場合による)


## 2. コーディングスタイルと品質基準
* **型ヒントの徹底:** すべての関数、メソッド、クラスの引数および戻り値に対して、Python標準の型ヒント（`typing` またはビルトイン）を明記すること。テンソルの形状（Shape）に関する情報はDocstringに記載すること。
* **Docstring:** すべての関数とクラスにGoogleスタイルのDocstringを記述し、引数、戻り値、例外（Raises）、および関数の目的を明記すること。
* **コメントの原則:** 「何をしているか（What）」ではなく「なぜそうしているか（Why）」を記述すること。コードを読めば自明な処理へのコメントは不要。
* **変更履歴の禁止:** コードの変更に影響するDocstring, readmeの部分変更は必要だが、そのコード変更に関する具体的な変更内容のコメントはコード上に残さない。
* **READMEの作成:** コードを動かすためのコマンドやtipsを記したREADMEを必ず作成し、コードの編集ごとに影響する箇所を必ず更新する.

## 3. 再現性の担保（Reproducibility）
実験ごとの揺らぎを排除するため、学習開始前に必ず以下のシード固定処理を実行すること。

* `torch.manual_seed()`
* `numpy.random.seed()`
* `random.seed()`
* GPUを使用する場合の決定論的動作の強制（`torch.backends.cudnn.deterministic = True` および `torch.backends.cudnn.benchmark = False`）

## 4. MLflowによるトラッキング（非ブラックボックス化）
暗黙的な状態を排除するため、`autolog` などの自動化ツールは使用せず、すべて手動でsqlight形式で明示的に記録すること。

* **パラメータの記録:** 学習率、バッチサイズ、エポック数などのハイパーパラメータは学習開始直後に `mlflow.log_params()` で記録すること。
* **メトリクスの記録:** エポックまたはステップごとに、Loss、Accuracy、勾配ノルム（Gradient Norm）などの数値を `mlflow.log_metric()` で記録すること。
* **アーティファクトの記録:** 実行時の設定ファイル（YAMLなど）やgitのコードバージョンIDを `mlflow.log_artifact()` で保存し、当時の実行状態を復元可能にすること。
* **tqdmを用いたprogressの表示** コンソールからも進行を確認できるようにtqdmを用いてprogressの表示を行う。

## 5. チェックポイントの管理と再開（Resume）
不測の事態での中断や追加学習に備え、完全に状態を復元できるチェックポイント機構を実装すること。

* **保存する状態:** 学習の最下に必要なすべての要素を1つの辞書にまとめて保存すること。
* **保存フロー:** 一時的にローカルへ `.pt` ファイルとして保存し、直後に `mlflow.log_artifact()` でMLflowの管理下に登録すること。
* **再開フロー:** 指定されたMLflowの `Run ID` から過去のアーティファクトをダウンロードし、状態を復元した上で、同じ `Run ID` に対してログを追記（Resume）できる設計にすること。
* **高精度モデルの保存** 学習内で最も精度の高い状態のモデルをテストとデプロイのための別で保存しておき、読み込める状態にしておく。

## 6. エラー処理と例外管理
長時間実行される学習プロセスを安全に管理するため、適切な例外処理を実装すること。

* **学習の中断（KeyboardInterrupt）:** ユーザーが `Ctrl+C` 等で学習を中断した場合、直ちに終了するのではなく、例外をキャッチしてその時点での最新状態をチェックポイントとして保存してから安全に終了（Graceful Shutdown）すること。
* **数値異常の検知:** Lossが `NaN` や `Inf` になった場合、または勾配が爆発した場合は、例外（`ValueError` 等）を発生させてエラーログを出力し、異常終了直前の状態を保存すること。
* **リソース解放:** エラー発生の有無に関わらず、`finally` ブロック等を用いてGPUメモリの解放（`torch.cuda.empty_cache()`）やファイルハンドルのクローズを確実に行うこと。